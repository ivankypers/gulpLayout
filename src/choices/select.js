const tourChoices = document.getElementById("tour");
const memberChoices = document.getElementById("tour-members");
const choicesTour = new Choices(tourChoices, {
    searchEnabled: false,
});
const choicesMember = new Choices(memberChoices, {
    searchEnabled: false,
});
